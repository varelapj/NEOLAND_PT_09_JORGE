import "./header.css"

export const Header = ({ children }) => {
    return (
     <header>  {children}</header>
    
      
     
    );
  }; 
  
//   import "./header.css"

//   export const Header = ({ children }) => {
//       return (
//           {children}
//       );
//     }; 
    
  